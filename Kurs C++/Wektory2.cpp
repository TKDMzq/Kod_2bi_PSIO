#include <iostream>
#include <vector>

using namespace std;

int main ()
{
   const int = 5;
   int a;
   vector <int> v(n);
   for (int i = 0; i < n; i++)
   {
       cout << "Podaj element nr " << i + 1 << " : ";
       cin >> a;
       v[i] = a;
       // TODO: Dopisanie wyswietlania
   } 
   return 0;
}
